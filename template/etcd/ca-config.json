{
  "signing": {
    "default": {
      "expiry": {{.}}
    },
    "profiles": {
      "www": {
         "expiry": "{{.}}",
         "usages": [
            "signing",
            "key encipherment",
            "server auth",
            "client auth"
        ]
      }
    }
  }
}